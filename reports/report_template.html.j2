<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Отчёт аудита: {{ profile.profile_name }}</title>
  <style>
    body { font-family: sans-serif; margin: 40px; }
    table { border-collapse: collapse; width: 100%; margin-top: 20px; }
    th, td { border: 1px solid #ccc; padding: 8px; }
    th { background: #eee; }
    .PASS { color: green; font-weight: bold; }
    .FAIL { color: red; font-weight: bold; }
    .WARN { color: orange; font-weight: bold; }
  </style>
</head>
<body>
  <h1>Отчёт аудита безопасности</h1>
  <p><strong>Профиль:</strong> {{ profile.profile_name }}<br>
     <strong>Описание:</strong> {{ profile.description }}<br>
     <strong>Дата:</strong> {{ date }}</p>

  <table>
    <thead>
      <tr><th>Проверка</th><th>Статус</th><th>Уровень</th><th>Детали</th></tr>
    </thead>
    <tbody>
    {% for item in results %}
      <tr>
        <td>{{ item.name }}</td>
        <td class="{{ item.result }}">{{ item.result }}</td>
        <td>{{ item.severity }}</td>
        <td><code>{{ item.output }}</code></td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <p><strong>Итог:</strong> {{ results | length }} проверок,
    <span style="color:green">PASS: {{ pass_count }}</span>,
    <span style="color:red">FAIL: {{ fail_count }}</span>,
    <span style="color:orange">WARN: {{ warn_count }}</span>
  </p>
</body>
</html>
