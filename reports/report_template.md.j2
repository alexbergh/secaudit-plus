# Отчёт SecAudit++

**Профиль:** {{ profile.profile_name }}  
**Описание:** {{ profile.description }}  
**Дата проведения:** {{ now.strftime("%Y-%m-%d %H:%M:%S") }}

---

{% for module, checks in results | groupby('module') %}
## Модуль: {{ module | capitalize }} ({{ checks | length }} проверок)

| №  | Название проверки                     | Severity | Результат    | Комментарий/Вывод                 |
|----|-------------------------------------|----------|--------------|----------------------------------|
{% for check in checks %}
| {{ loop.index }} | {{ check.name }}                   | {% if check.severity == 'high' %}**🔴 {{ check.severity|upper }}**{% elif check.severity == 'medium' %}🟠 {{ check.severity|capitalize }}{% else %}🟢 {{ check.severity|capitalize }}{% endif %} | {% if check.result == 'PASS' %}✅ Пройдено{% elif check.result == 'FAIL' %}❌ Не пройдено{% else %}⚠️ Неопределён{% endif %} | ```\n{{ check.output | trim }}\n``` |
{% endfor %}

{% endfor %}
